document.getElementById("myButton").addEventListener("click", function() {
  var text = "Rossiya";
  var index = 0;
  var outputElement = document.getElementById("output");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton1").addEventListener("click", function() {
  var text = "Ukraina";
  var index = 0;
  var outputElement = document.getElementById("output1");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton2").addEventListener("click", function() {
  var text = "Belarussiya";
  var index = 0;
  var outputElement = document.getElementById("output2");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton3").addEventListener("click", function() {
  var text = "Polonia";
  var index = 0;
  var outputElement = document.getElementById("output3");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton4").addEventListener("click", function() {
  var text = "Polsha";
  var index = 0;
  var outputElement = document.getElementById("output4");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton5").addEventListener("click", function() {
  var text = "Moldova";
  var index = 0;
  var outputElement = document.getElementById("output5");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton6").addEventListener("click", function() {
  var text = "Ruminiya";
  var index = 0;
  var outputElement = document.getElementById("output6");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton7").addEventListener("click", function() {
  var text = "Balgariya";
  var index = 0;
  var outputElement = document.getElementById("output7");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton8").addEventListener("click", function() {
  var text = "Serbiya";
  var index = 0;
  var outputElement = document.getElementById("output8");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton9").addEventListener("click", function() {
  var text = "Slovakiya";
  var index = 0;
  var outputElement = document.getElementById("output9");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton10").addEventListener("click", function() {
  var text = "Chexiya";
  var index = 0;
  var outputElement = document.getElementById("output10");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton11").addEventListener("click", function() {
  var text = "Avfstriya";
  var index = 0;
  var outputElement = document.getElementById("output11");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton12").addEventListener("click", function() {
  var text = "Almaniya";
  var index = 0;
  var outputElement = document.getElementById("output12");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton13").addEventListener("click", function() {
  var text = "Paysos Baxes";
  var index = 0;
  var outputElement = document.getElementById("output13");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton14").addEventListener("click", function() {
  var text = "Italiya";
  var index = 0;
  var outputElement = document.getElementById("output14");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton15").addEventListener("click", function() {
  var text = "Shveysariya";
  var index = 0;
  var outputElement = document.getElementById("output15");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton16").addEventListener("click", function() {
  var text = "Fransiya";
  var index = 0;
  var outputElement = document.getElementById("output16");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton17").addEventListener("click", function() {
  var text = "Ispaniya";
  var index = 0;
  var outputElement = document.getElementById("output17");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton18").addEventListener("click", function() {
  var text = "Portugaliya";
  var index = 0;
  var outputElement = document.getElementById("output18");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton19").addEventListener("click", function() {
  var text = "Buyuk Britaniya";
  var index = 0;
  var outputElement = document.getElementById("output19");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton20").addEventListener("click", function() {
  var text = "Irlandiya";
  var index = 0;
  var outputElement = document.getElementById("output20");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton21").addEventListener("click", function() {
  var text = "Norvegiya";
  var index = 0;
  var outputElement = document.getElementById("output21");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton22").addEventListener("click", function() {
  var text = "Norvegiya";
  var index = 0;
  var outputElement = document.getElementById("output22");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton23").addEventListener("click", function() {
  var text = "Firlandiya";
  var index = 0;
  var outputElement = document.getElementById("output23");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton24").addEventListener("click", function() {
  var text = "Estoniya";
  var index = 0;
  var outputElement = document.getElementById("output24");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton25").addEventListener("click", function() {
  var text = "Letoniya";
  var index = 0;
  var outputElement = document.getElementById("output25");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton26").addEventListener("click", function() {
  var text = "Lituaniya";
  var index = 0;
  var outputElement = document.getElementById("output26");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton27").addEventListener("click", function() {
  var text = "Serbiya";
  var index = 0;
  var outputElement = document.getElementById("output27");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});
document.getElementById("myButton28").addEventListener("click", function() {
  var text = "Turkiya";
  var index = 0;
  var outputElement = document.getElementById("output28");
  outputElement.textContent = ""; // Clear existing text

  var typingInterval = setInterval(function() {
      if (index < text.length) {
          outputElement.textContent += text.charAt(index);
          index++;
      } else {
          clearInterval(typingInterval);
      }
  }, 100); // Controls typing speed
});